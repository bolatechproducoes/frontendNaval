// Importa o metódo createBrowserHistory do history
import { createBrowserHistory } from 'history';

// Cria a constante history que executa o metódo createBrowserHistory
const history = createBrowserHistory();

// Exporta como padrão a constante history
export default history;
